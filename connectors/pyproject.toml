[project]
name = "translink-connectors"
version = "0.1.0"
description = "AWS Lambda connectors — ingest Translink GTFS-RT feeds to Databricks via Zerobus"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "databricks-zerobus-ingest-sdk>=0.3.0",
    "protobuf>=4.25.0,<7.0",
    "requests>=2.28.1",
]

[dependency-groups]
dev = [
    "grpcio-tools>=1.60.0",
    "python-dotenv>=1.2.1",
]
