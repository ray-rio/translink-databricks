# =============================================================================
# Central project configuration — single source of truth for ALL settings
# and secrets.
#
# Copy this file and fill in your values:
#   cp config.yml.example config.yml
#
# After editing, regenerate downstream config files:
#   uv run scripts/sync_config.py
#
# This regenerates:
#   - connectors/.env.example           (masked secrets, committed to git)
#   - connectors/samconfig.toml.example (masked secrets, committed to git)
#   - connectors/.env                   (real values, gitignored)
#   - connectors/samconfig.toml         (real values, gitignored)
#   - analytics/databricks.yml          (gitignored)
# =============================================================================

databricks:
  workspace_url: https://<workspace>.cloud.databricks.com
  workspace_id: "<workspace-id>"
  catalog: "<catalog-name>"
  schema: "<schema-name>"
  client_id: "<service-principal-app-id>"
  client_secret: "<service-principal-secret>"

aws:
  region: ap-southeast-2
  stack_name: transit-connector

translink:
  vehicle_positions_url: https://gtfsrt.api.translink.com.au/api/realtime/SEQ/VehiclePositions
  trip_updates_url: https://gtfsrt.api.translink.com.au/api/realtime/SEQ/TripUpdates
  gtfs_static_url: https://gtfsrt.api.translink.com.au/GTFS/SEQ_GTFS.zip

log_level: INFO
